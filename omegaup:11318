#include <iostream>
using namespace std;
int main(){
   cout << "ingrese A: ";
   int A, B;
   cin >> A;
   cout << "ingrese B: ";
   cin >> B;
   if(A+B==5){
    B=B +3;
    cout << "resultado: " << (2*A+B) << endl;
   } else {
    --A;
    if(((7*A+B)%2)==0){
        cout << "resultado: "<< (A-B);
    } else {
        cout << "resultado: " << (A*B);
    }
   }
    return 0;
}
